# Tarea 1

En esta tarea se nos pide que creemos una funcion que se capaz de devolver los ancestros de un procesos.

# Archivos modificados:
* kernel/sysproc.c
* kernel/syscall.c
* kernel/syscall.h
* user/user.h
* user/usys.pl

La definicion de la funcion getancestor, esta definida en el archivo **kernel/sysproc.c** Aqui encontramos uno de los primeros problemas, ya que hacer la implementacion de una funcion sin argumentos es bastante directo, sin embargo, para poder pasar argumentos a las syscalls, hay que usar una especie de funcion que los define, como por ejemplo argint().

la declaracion de la funcion esta puesta en el archvo **kernel/syscall.c**, archivo en el cual tambine esta el mapeo de la funcion con su numero, definido en **syscall.h** 

luego, pasando a la parte del usuraio, nos encontramos con el archivo **user/user.h** en el cual esta declarada la llamada del usuario que va a represetar a la syscall que hemos creado, posteriormente añadiremos una entrada a **kernel/usys.pl**, el cual termina de enalzar la llamada de istema con su corespondeitne funcion del usuario.


# Programa de Prueba
Para hacer las prubeas se obto por ejecutar varias veces la funcion para comprobar su funcionamiento en el arvhivo **yoSoyTuPadre.c**, el cual debe ser agragado en el **makefile** en la parte de **UPROGS**
